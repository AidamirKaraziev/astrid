---
этап: набор скиллов учёта прогресса
статус: закрыт
дата: 2026-09-08
план: .claude/plan/roadmap.md
---

# Передача: учёт прогресса развёрнут, план размечен, работа над продуктом не начиналась

## Сделано и проверено

- Набор скиллов в `~/.claude/skills` — git-репозиторий, теги `v1.0.0`, `v1.2.0`,
  `v1.2.1`. Новые: `/plan`, `/status`, `/note`, `/track-init`. Переписаны:
  `stage-start` 1.2.0, `stage-end` 1.2.0, `commit-message` 2.0.0,
  `save-session` 2.1.0.
- Скрипты считают цифры вместо модели: `lib/spend.py` (часы и токены по
  областям из транскриптов), `lib/counters.py` (счётчики `n/m`),
  `lib/ledger_commit.py` (хук `post-commit`).
- Сквозная проверка на временном репозитории прошла: тик чекбокса → счётчик
  стал `2/3` → коммит → хеш попал в `ledger.jsonl`.
- `spend.py` прогнан на реальных транскриптах Astra: 39 сессий, 46.5 ч, 73.3M
  свежих токенов. Все вне учёта — разметка начинается с этой строки ledger.
- В Astra развёрнуто: `.claude/plan/` с roadmap на 9 эпиков, подплан
  `E01-подарки-до-людей.md` на 4 этапа, пустой `ledger.jsonl`, хук `post-commit`.
- Тесты проекта **не гонялись** — код Astra в этой сессии не менялся.

## Не доделано

- Подпланы эпиков E02–E09 не нарезаны. Это сознательно: режем тот эпик, за
  который берёмся.
- Шесть кандидатов в эпики лежат в разделе «Не в плане» roadmap — решения по
  ним нет.
- Знание сессии в vault не записано. Развилок было две, обе с отброшенной
  альтернативой: журнал вместо ретроспективного сканирования; развилка вместо
  автоцепочки `save-session`.

## Следующий этап

**Цель.** S01 эпика E01 — прогнать бота вживую по сценариям подарков.

**Готово, когда.** Пройдены сценарии из `HANDOFF-clean-chat-and-gifts.md` плюс
четыре подарочных: подарить и отправить кнопкой; забрать со второго аккаунта и
нажать «Открыть подарок»; отозвать ссылку и открыть её; открыть подарочную
ссылку уже зарегистрированным. Найденное либо починено, либо записано этапом.

## Первые шаги

1. `/stage-start` — он сам возьмёт S01 и его критерий из подплана.
2. Открыть `HANDOFF-clean-chat-and-gifts.md` — оттуда базовые сценарии.
3. Второй Telegram-аккаунт нужен обязательно: без него половина сценариев не
   проверяется.

## Не трогать

- Подпланы E02–E09 — не нарезать заранее.
- `discount_percent` в боевом каталоге — это S02, отдельный этап.
- Мёрж в `main` и миграции на прод — S03 и S04, строго после прогона.

## Уточнить перед стартом

- S01 может не влезть в одну сессию: девять сценариев плюс починка найденного.
  Не влезает — закрывать со статусом `в работе`, это штатный исход.

## Ссылки

- `.claude/plan/E01-подарки-до-людей.md` — четыре этапа с критериями приёмки.
- `~/.claude/skills/README.md` — как скиллы ложатся в рабочий процесс.
