---
tags: [home, priorities]
date: 2026-07-16
---

# Текущие приоритеты

Обновляется в конце каждой сессии (команда: «сохрани сессию» / «заверши сессию»).

## В фокусе

→ **Таро-трек** (`feature/tarot-track`, 5 коммитов 2026-07-16): колода 78 карт,
изображения Райдер-Уэйт, расклады «Да/Нет» / «3 карты» / «Отношения» через
RabbitMQ-пайплайн, лимит 1/день, роутинг из AI-чата. Осталось: **TARO-6 —
миграция 012 + E2E на deadtiger** (см. `spec/backlog.md`, решение —
[[таро гибридный пайплайн карты мгновенно интерпретация из worker]]).

## Было в фокусе раньше

→ **Единый бэклог:** `spec/backlog.md`  
→ **Совместимость:** [[пайплайн совместимости промпт LLM PDF worker]] · [[Split Contract LLM и Pydantic для совместимости]]

0. **Деплой удаления Ollama на deadtiger** — локальная LLM удалена из кода (2026-07-08); перед `git pull` проверить `DEEPSEEK_ENABLED=true` + `DEEPSEEK_API_KEY` в prod `.env`, затем `docker compose up -d --build --remove-orphans` и `docker volume rm astra_ollama_data`. Чек-лист: [[локальная LLM Ollama удалена весь daily на DeepSeek]].
1. **Деплой на deadtiger** — worker + api с фиксами импортов и caption tldr; `docker compose up -d --build api worker`.
2. **E2E совместимость** — полный цикл: FSM → PDF в TG с `tldr` в caption.
3. **Split Contract LLM ↔ Pydantic** — assemble уже в коде; добить стабильность валидации на реальных парах.
4. **UX при фейле LLM** — уведомление в TG (как у daily prediction).

## Сделано в последней сессии (2026-07-06)

- Приветственное видео Astrid в `/start` — только для новых пользователей (`is_new_user`), возвращающиеся получают как раньше только текст.
- Сжатие видео: 9.6 МБ (15.3 Мбит/с) → 904 КБ (720p, CRF 23, `faststart`) без потери качества на глаз.
- Кэш `file_id` в Redis — видео заливается один раз, дальше рассылается по `file_id` (мгновенно). Паттерн → [[кэш file_id вместо повторной заливки медиа в Telegram]].
- Тексты для BotFather (short description / About / приветствие) — придуманы, но не применены, ждут ручного `/setdescription` и `/setabouttext`.
- См. [[2026-07-06 приветственное видео Astrid для новых пользователей]].

## Сделано раньше (2026-07-03)

- Caption PDF: `tldr` из LLM + заголовок пары (`format_compatibility_pdf_caption`).
- Фикс `date` import в FSM совместимости (API падал на confirm).
- Фикс `dispatch_task` import в worker consumer (RabbitMQ задачи не выполнялись).
- См. [[2026-07-03 caption tldr PDF и фиксы импортов worker бота]].

## Утверждено ранее

- **Продукт:** полная синастрия пары + PDF
- **Оплата:** MVP бесплатно, Stars после PR4
- **LLM совместимости:** DeepSeek (облако)
- **RabbitMQ:** всегда, без inline fallback

## Отложено

- Mini App, Web/mobile
- Stars / монетизация совместимости
- LLM A/B temperature для daily

## Блокеры

- Промпт/валидация совместимости — часть фейлов на реальных парах (см. логи worker).
- `TELEGRAM_SUPPORT_USERNAME` не задан в prod `.env` (если актуально).
- VPN/прокси на deadtiger.

## Следующая сессия

- Подтвердить E2E на prod после ребилда api/worker.
- При необходимости — доработать Split Contract / снизить % validation fail.
- Вручную проверить приветственное видео на реальном Telegram-аккаунте после `make up` на deadtiger.
- Вбить тексты BotFather (`/setdescription`, `/setabouttext`) — готовые варианты в сессии [[2026-07-06 приветственное видео Astrid для новых пользователей]].
