---
tags: [home, priorities]
date: 2026-08-08
---

# Текущие приоритеты

Только то, что **открыто**. Готовое и задеплоенное сюда не пишем — оно живёт
в `knowledge/` и в истории git. Обновляется скиллом `save-session`;
не дописывать слоями, а переписывать.

## Сейчас в работе

→ **Тексты бота по канону.** Правила записаны и применены к коду:
[[текст бота сканируется сверху вниз и звучит голосом Астрид]]. Область —
только строки в коде; промпты LLM и ИИ-редактор рассылок по канону **не
переписаны**, это следующий заход.

## Ранее в фокусе

- **Регистрация без данных рождения** — сделано и на `main` (`3ca1c83`,
  миграция `029_profile_without_birth_data`). Профиль заводится без даты,
  времени и места; данные запрашиваются в момент, когда продукт их требует
  (`telegram/birth_data_gate.py`). Миграцию на проде не накатывали.

## Открыто

- **`main` на три коммита впереди `origin/main`** — не запушено. Ветка
  `feat/profile-without-birth-data` смёржена, но не удалена.
- **Канон не распространён на генерацию.** Промпты гороскопов, таро и
  «Спроси Астрид» пишутся по своим правилам; переложить в них голос,
  структуру и правило про род — отдельная работа.
- **Аудит текстов живёт скриптом в скретчпаде**, не в репозитории. Нужен
  постоянно — оформить как тест или как отдельную команду.

- **Служба заботы — содержание.** Тексты переписаны по канону и переведены
  на «ты» (`9c0b6ff`), но набор тем, SLA и edge-кейсы всё ещё черновик.
  → [[служба заботы v1 работает но содержание черновик]] (inbox)
- **«Спроси Астрид» — наполнение.** Готовы 5 вопросов; остальные темы по
  шаблону [[продукт раздела Спроси Астрид собирается по одному шаблону]].
  Дальше — платный апдейт «что изменилось сейчас» по транзитам.
- **Монетизация натала и совместимости.** Код обоих готов на `main`, продукты
  не выставлены в каталог. Нужен E2E совместимости на проде (FSM → PDF) и
  решение по цене.
- **Первая живая рассылка** не проводилась — механика собрана, боевого
  прогона не было.
- **Админ-панель: «Люди» и «Поддержка»** — пока макеты (`admin/mockups.py`).
- **AI-чат Astrid** — код на `main`, флаг `ai_chat_enabled=False`. Включать
  или похоронить: [[AI-чат Astrid через structured output и роутинг в FSM]].
- **Этап 2 оплаты** — карта/ЮKassa через внешнюю платёжную страницу.
  Архитектура готова: provider + рублёвые цены в каталоге.
- **База знаний:** сессии 30 июля — 6 августа не записаны (портрет профиля,
  настенные часы, справочник городов СНГ). Заполнить при следующем
  `save-session`.
- **Инженерная гигиена и AI-глубина** — приоритет 1 учебного плана
  [[навыки Senior AI Engineer осваиваю задачами внутри Astrid]], проверками
  подтверждён как не начатый. Четыре задачи, все нужны проекту сами по себе:
  - `[tool.ruff]` и `[tool.mypy]` в `pyproject.toml` плюс шаги lint, mypy и
    порог coverage в `.github/workflows/ci.yml` — сейчас в CI только `pytest`;
  - рейт-лимит и бюджетный потолок на пользователя (Redis уже стоит, проверка
    перед публикацией задачи в RabbitMQ) — сейчас лимита нет нигде, платный бот
    открыт на слив бюджета DeepSeek;
  - eval-харнесс: golden set на 40 реальных карт поверх
    `tests/compatibility_llm_samples.py` и валидаторов `llm/prompts/*/base.py`,
    отдельным job в CI, с отчётом «промпт vN против vN+1»;
  - RAG на pgvector над `astra-vault` (миграция с `CREATE EXTENSION vector`,
    чанкинг по заголовкам, BM25 плюс косинус, recall@k и MRR на ≥30 размеченных
    вопросах).
- **Цифры продукта.** Механика есть (`admin/metrics.py`, `activity_days`,
  `llm_calls`), чисел нет ни одного. Нужны DAU, когорты D1/D7/D30, конверсия в
  оплату и стоимость модели на пользователя — без них оценка Astrid как бизнеса
  держится на догадках.

## Блокеры

- Промпт/валидация совместимости — часть фейлов на реальных парах (логи worker).
- VPN/прокси на deadtiger (mihomo, ручное обновление подписки).

## Утверждено — не переспрашивать

- **Продукт:** карта дня и ежедневные прогнозы бесплатно; платное — таро,
  «Спроси Астрид»; дальше натал и совместимость.
- **Оплата:** Telegram Stars, каталог `products`/`product_prices` в БД.
- **LLM:** только облачный DeepSeek, локальная удалена.
- **RabbitMQ:** всегда для LLM-задач, без inline fallback.
- Полный портфель идей → [[портфель монетизации Astra]]; бэклог → `spec/backlog.md`.

## Отложено

- Mini App, Web/mobile.
- Перевёрнутые карты таро (`reversed` в LLM не передаётся — механика этапа 2).
- LLM A/B temperature для daily.
