---
tags: [home, priorities]
date: 2026-07-29
---

# Текущие приоритеты

Обновляется в конце каждой сессии (команда: «сохрани сессию» / «заверши сессию»).

## В фокусе

→ **Админ-панель `/admin`** — восемь разделов на `main`, 825 тестов.
Живые: каталог (цены, скидки, призы колеса), очередь проблем, платежи,
настройки (прайс моделей), метрики, рассылки. Макеты: «Люди», «Поддержка».
Доступ снаружи через Tailscale — `http://100.120.8.76:8000/admin`
([[Tailscale вместо публичного домена для админ-панели]]). Панель собрана
так, чтобы уехать в свой сервис двумя правками
([[панель выносится в отдельный сервис одной строкой]]).
**Открыто:** деплой (миграции 022, 023, 024), `ADMIN_TEST_CHAT_ID` в `.env`,
сверить прайс моделей с сайтами провайдеров, разделы «Люди» и «Поддержка».
См. [[2026-07-29 админ-панель разделы метрики очередь и рассылки]].

→ **Метрики и учёт** — журнал использования продуктов, дни активности,
вызовы моделей с токенами и себестоимостью. Дашборд по дням/неделям/месяцам,
ежедневная сводка в группу операторов в 10:00. Серию дней теперь двигает
любое действие, а не только `/start`
([[активность пишем одной строкой в день с двумя датами]],
[[себестоимость LLM считается в момент вызова]]).
**Важно:** история копится с момента деплоя, задним числом не восстановится.

→ **Рассылки** — аудитория по фильтрам с исключениями, ИИ-редактор голосом
Астрид, телеграмный предпросмотр, пробная отправка себе и подтверждение,
история с повтором недошедшим. Шлёт воркер, 20 сообщений в секунду.
**Открыто:** первая живая рассылка не проводилась.

## Ранее в фокусе

→ **Служба заботы (поддержка в боте)** — v1 собрана на `main` (коммит
`850c23a`, 2026-07-25). Гибрид: кнопка «💬 Помощь» + `/help`/`/paysupport`
+ контекстные подсказки → FAQ-самопомощь → релей к живому оператору через
**закрытую админ-группу** (карточка обращения с `charge_id` последней
покупки; оператор отвечает reply → бот доставляет клиенту в бот). Убрана
коллизия имени «Астрид». v1 **без AI** (осознанно). Миграция 018
`support_tickets`. 531 тест. `.env` уже настроен
(`TELEGRAM_ADMIN_GROUP_ID=-1004419830379` — супергруппа,
`TELEGRAM_SUPPORT_USERNAME=astrid_team`); проверено, что бот пишет в
группу. См. [[служба заботы гибрид FAQ и релей через админ-группу]] и
[[2026-07-25 служба заботы FAQ и релей обращений через админ-группу]].
**Открыто:** довести до ума **содержание** (тексты/наполнение FAQ) —
[[служба заботы v1 работает но содержание черновик]]. И фаза 2 — AI-триаж.

→ **Таро-трек** — реализован полностью на **`main`** (НЕ на
`feature/tarot-track`). Колода 78 карт, изображения Райдер-Уэйт, расклады
**«Загадай желание»** (3 карты + вердикт + срок, заменил «На решение») /
«Три карты» (Сердце→Скрытое→Исход) / **«Отношения» → фреймворк
«Что он/она чувствует» (вариант A)**. Интерпретация —
**структурированный JSON, промпт и схема на каждый продукт**
([[таро расклады структурированный JSON и промпт на продукт]]), гибридный
пайплайн ([[таро гибридный пайплайн карты мгновенно интерпретация из worker]]),
роутинг из AI-чата.

**Последнее (2026-07-21): расклады стали платными — Telegram Stars.**
Все три расклада по 50 ⭐ (бесплатной осталась «Карта дня»): вопрос → инвойс →
оплата → карты → интерпретация. Каталог `products`/`product_prices` в БД
(мультивалютность: новая валюта = INSERT), скидки с кнопкой
`5̶0̶ ⭐ → 5 ⭐ · скидка −90%` (вариант B), авто-refund при фейле LLM,
`/paysupport`, лимит 1/день и бонусы удалены. Единая миграция 013. 426 тестов.
См. [[2026-07-21 оплата таро Telegram Stars каталог цен скидки]] и
[[цены и скидки товаров в БД каталог товар на валюту со снапшотом в платеже]].
Ранее: [[2026-07-19 таро замена На решение на Загадай желание]],
[[2026-07-18 таро Отношения вариант A Что чувствует второй]].

## Осталось

- **Спроси Астрид — миграция и E2E:** `alembic upgrade head` (накатит 019
  `ask_readings` + товар `ask_love_fated_count` за 1 ⭐) → живая покупка:
  вопрос → статус → инвойс → карточка с числом → разбор → шаринг → архив.
- **Спроси Астрид — наполнение:** остальные 7 тем (вопросы в стиле с «?»),
  затем платный апдейт «что изменилось сейчас» по транзитам. Шаблон работы —
  [[продукт раздела Спроси Астрид собирается по одному шаблону]].
- **Служба заботы — деплой и E2E:** `git pull` → `alembic upgrade head`
  (накатит 018 `support_tickets`) → перезапуск → живой тест полного цикла
  (Помощь → написать → карточка в группе → reply оператора → ответ
  клиенту). Затем доработать **содержание FAQ**
  ([[служба заботы v1 работает но содержание черновик]]).
- **Живой E2E оплаты Stars** (реальный аккаунт: инвойс → оплата → карты →
  интерпретация → refund при фейле) + рендер юникод-зачёркивания на устройствах.
- **Деплой на deadtiger:** `git pull` (первый pull после удаления Ollama —
  чек-лист ручных шагов в [[локальная LLM Ollama удалена весь daily на DeepSeek]]:
  `DEEPSEEK_ENABLED` + `DEEPSEEK_API_KEY` в prod `.env`, `--remove-orphans`,
  снос volume ollama), затем `alembic upgrade head` (накатит 012 + 013);
  `TELEGRAM_SUPPORT_USERNAME` в prod `.env` (нужен для `/paysupport`).
- **Ветки/push:** `main` впереди `origin/main` (не запушено); решить судьбу
  устаревшей `feature/tarot-track`.
- **Ручное после деплоя:** проверить приветственное видео на реальном аккаунте;
  вбить тексты BotFather (`/setdescription`, `/setabouttext`) — готовые варианты
  в [[2026-07-06 приветственное видео Astrid для новых пользователей]].
- Открытый вопрос: `reversed` в LLM не передаётся (`base.py::_cards_payload`) —
  кросс-продуктовая правка, если читать перевёрнутые карты.
- Попутный баг вне таро: `publish_natal_chart` TypeError (отдельный чип).
- Этап 2 монетизации: карта/ЮKassa через внешнюю платёжную страницу
  (архитектура payments уже готова: provider + рублёвые цены в каталоге).

## Треки на паузе

- **Совместимость:** E2E на prod (FSM → PDF с `tldr` в caption), стабильность
  Split Contract / % validation fail на реальных парах, UX-уведомление при
  фейле LLM. См. [[пайплайн совместимости промпт LLM PDF worker]] ·
  [[Split Contract LLM и Pydantic для совместимости]].
- **Разбор натала** — готов на `feature/natal-report`, ждёт миграции и E2E.
- Единый бэклог: `spec/backlog.md`.

## Утверждено

- **Продукт:** ежедневные прогнозы бесплатно; платные — таро (Stars, этап 1),
  дальше совместимость/натал; этап 2 — карта/ЮKassa.
- **LLM:** только облачный DeepSeek, локальная LLM удалена
  ([[локальная LLM Ollama удалена весь daily на DeepSeek]]).
- **RabbitMQ:** всегда для LLM-задач, без inline fallback.

## Отложено

- Mini App, Web/mobile.
- Монетизация совместимости и натала (после E2E таро-оплаты).
- LLM A/B temperature для daily.

## Блокеры

- Промпт/валидация совместимости — часть фейлов на реальных парах (логи worker).
- VPN/прокси на deadtiger (mihomo, ручное обновление подписки).
