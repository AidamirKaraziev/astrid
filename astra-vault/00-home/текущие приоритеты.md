---
tags: [home, priorities]
date: 2026-07-02
---

# Текущие приоритеты

Обновляется в конце каждой сессии (команда: «сохрани сессию» / «заверши сессию»).

## В фокусе

→ **Единый бэклог:** `spec/backlog.md`  
→ **Совместимость:** [[пайплайн совместимости промпт LLM PDF worker]] · [[Split Contract LLM и Pydantic для совместимости]]

1. **Split Contract LLM ↔ Pydantic** — утвердить план, реализовать `CompatibilityLlmRaw` + `assemble_llm_output` + автопромпт из схемы.
2. **Деплой на deadtiger** — коммит с пайплайнами, `alembic upgrade head`, перезапуск worker+api.
3. **E2E совместимость** — полный цикл после фикса валидации.
4. **UX при фейле LLM** — уведомление в TG (как у daily prediction).

## Сделано в последней сессии (2026-07-02)

- Staged progress (prediction + compatibility), typing, Redis progress.
- Пайплайны RabbitMQ: daily 4 этапа, compatibility 4 этапа.
- RabbitMQ обязателен; фикс publisher cache.
- PDF filenames, удаление разборов (карточка + кнопки).
- Диагностика prod: LLM validation fail (орбы, длина строк) — не инфра.
- См. [[2026-07-02 staged progress pipelines и диагностика LLM совместимости]].

## Утверждено ранее

- **Продукт:** полная синастрия пары + PDF
- **Оплата:** MVP бесплатно, Stars после PR4
- **LLM совместимости:** DeepSeek (облако)
- **RabbitMQ:** всегда, без inline fallback

## Отложено

- Mini App, Web/mobile
- Stars / монетизация совместимости
- LLM A/B temperature для daily

## Блокеры

- Промпт/валидация совместимости — ~30% фейлов на реальных парах (см. логи worker).
- `TELEGRAM_SUPPORT_USERNAME` не задан в prod `.env` (если актуально).
- VPN/прокси на deadtiger.

## Следующая сессия

- Утвердить [[Split Contract LLM и Pydantic для совместимости]].
- Реализовать assemble + тесты.
- Задеплоить и прогнать E2E.
