---
tags: [session, tarot]
date: 2026-07-18
---

# 2026-07-18 — таро-расклады: JSON на продукт, «Три карты» → Сердце/Скрытое/Исход

Продолжение таро-трека. Начинали с идеи продуктов и конкурентов, дошли до
рабочих раскладов, вычистили баги и переработали архитектуру интерпретации.

## Что сделано (коммиты на `main`, tip `adb3884`)

1. **Пол и имя клиента в интерпретации** (`0e1094f`) — `build_*_user_message`
   получает `user_name`/`gender` из профиля; согласование рода в промпте.
2. **Astrid → Астрид во всём боте** (`ffe8271`) — промпты (прогноз, зодиак,
   AI-чат) + видимые строки; `BTN_ASK_ASTRID_LEGACY` для закэшированных клавиатур.
3. **Кнопка «Сделать ещё расклад» на лимите** (`5777577`) — бонусный расклад
   сверх лимита, счётчик в Redis (TTL 48ч), задел под Stars; для всех раскладов.
4. **Фикс: sanitize прогноза ломал расклады** (`5bd7ddb`) →
   [[sanitize прогноза схлопывал многоблочные расклады таро]].
5. **`make worker`** (`f93cc8b`) — воркер на хосте в терминале, инфра в докере.
6. **Рефакторинг: промпт и схема на каждый расклад, вывод LLM — JSON** (`3d61726`)
   → [[таро расклады структурированный JSON и промпт на продукт]].
7. **«Три карты» → Сердце → Скрытое → Исход** (`adb3884`) — вместо плоского
   Прошлое/Настоящее/Будущее; тематические эмодзи позиций (💛/🌊/🔮).

## Ключевой урок

Парсинг абзацев LLM «вслепую» (сшивать N+1 блоков с позициями) — хрупко:
вступление модели или лишний перенос строки всё ломали. Перешли на
**структурированный JSON: поле на позицию** → съехать физически нельзя.
Заодно вердикт «Да/Нет» стал отдельным полем.

## Состояние веток (важно)

- Вся работа таро-трека — на **`main`** (tip `adb3884`), НЕ на `feature/tarot-track`
  (та осталась указателем на `ad5a97f`).
- `main` на 4 коммита впереди `origin/main` — **не запушено**.

## Осталось (не в этой сессии)

- **TARO-6:** `alembic upgrade head` (миграция 012) + E2E всех трёх раскладов на
  реальном Telegram (через `make worker`), проверка resumability.
- Решить судьбу ветки `feature/tarot-track` (влить/удалить) и push `main`.
- Попутный баг вне таро: `publish_natal_chart` TypeError (чип висит отдельно).

## Связи

- [[таро гибридный пайплайн карты мгновенно интерпретация из worker]]
- [[таро расклады структурированный JSON и промпт на продукт]]
- [[sanitize прогноза схлопывал многоблочные расклады таро]]
- `spec/backlog.md` → раздел «Таро-трек» (TARO-1…7)
